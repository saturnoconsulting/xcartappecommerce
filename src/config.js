import Constants from 'expo-constants';

const { apiUrl }=Constants.expoConfig.extra;
const { licenseKey }=Constants.expoConfig.extra;
const { idsalespointkey }=Constants.expoConfig.extra;

export const API_URL=apiUrl;
export const LICENSE=licenseKey;
export const IDSALESPOINT=idsalespointkey;